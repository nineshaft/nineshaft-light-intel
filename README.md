# nineshaft-light-intel
Asynchronous, architecture-clean OSINT profile tracker written in C# (.NET 10). Implements strict input validation, connection pooling (anti-socket exhaustion), and user-agent rotation.
